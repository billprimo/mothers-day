{
  "rules": {
    ".read": "auth != null",
    ".write": "auth != null",
    "messages": {
      "$message_id": {
        ".read": "auth != null",
        ".write": "auth != null",
        ".validate": "newData.hasChildren(['message', 'approved'])"
      }
    }
  }
}
